{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://researchartifacts.github.io/data-schemas/schemas/paper_index.schema.json",
  "title": "Paper Index",
  "description": "Canonical paper index with stable integer IDs. Generated by generate_paper_index.py from authors.yml data. Each unique paper is stored once; authors reference papers by ID.",
  "type": "array",
  "items": {
    "$ref": "#/$defs/Paper"
  },
  "$defs": {
    "Paper": {
      "type": "object",
      "title": "Paper",
      "description": "A unique paper associated with tracked conferences.",
      "required": [
        "id",
        "title",
        "normalized_title",
        "conference",
        "year"
      ],
      "properties": {
        "id": {
          "type": "integer",
          "description": "Stable integer ID for this paper. Preserved across pipeline runs.",
          "minimum": 1
        },
        "title": {
          "type": "string",
          "description": "Original paper title as found in DBLP."
        },
        "normalized_title": {
          "type": "string",
          "description": "Lowercased title with punctuation removed, used for deduplication matching."
        },
        "conference": {
          "type": "string",
          "description": "Conference abbreviation."
        },
        "year": {
          "type": ["integer", "null"],
          "description": "Publication year.",
          "minimum": 2017,
          "maximum": 2030
        },
        "category": {
          "type": "string",
          "description": "Research domain category.",
          "enum": ["systems", "security", "unknown", ""]
        },
        "badges": {
          "type": "array",
          "description": "Artifact evaluation badges for this paper.",
          "items": {
            "type": "string"
          }
        },
        "artifact_citations": {
          "type": "integer",
          "description": "Number of citations to this paper's artifact.",
          "minimum": 0
        },
        "has_artifact": {
          "type": "boolean",
          "description": "Whether this paper has an associated artifact (false for papers_without_artifacts)."
        }
      },
      "additionalProperties": false
    }
  }
}
