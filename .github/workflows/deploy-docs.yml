name: Generate & Deploy Schema Docs

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0        # need full history for tags

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Restore cached docs
        uses: actions/cache@v5
        with:
          path: docs
          key: schema-docs-${{ github.sha }}
          restore-keys: |
            schema-docs-

      - name: Validate schemas (current)
        run: |
          python -c "
          import json, pathlib, jsonschema
          for f in sorted(pathlib.Path('schemas').glob('*.schema.json')):
              schema = json.loads(f.read_text())
              jsonschema.Draft202012Validator.check_schema(schema)
              print(f'  ✓ {f.name}')
          print('All schemas valid.')
          "

      - name: Generate documentation (all versions)
        run: bash generate_docs.sh

      - uses: actions/upload-pages-artifact@v5
        with:
          path: docs

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
